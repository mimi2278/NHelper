{
  "name": "novel-helper",
  "version": "0.1.1",
  "description": "Obsidian Novel Helper plugin",
  "main": "main.js",
  "scripts": {
    "build": "tsc main.ts --module commonjs --target ES2018 --moduleResolution node --esModuleInterop --skipLibCheck --outDir .",
    "check": "tsc -noEmit -skipLibCheck"
  },
  "devDependencies": {
    "obsidian": "latest",
    "typescript": "^5.4.0"
  }
}
